# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  database: spree_development_one_three

test:
  adapter: postgresql
  database: spree_test_one_three
  username: postgres
  min_messages: warning
